{
  "name": "animated-qubits-gh-pages",
  "version": "0.0.0",
  "description": "Web site for animated-qubits",
  "main": "quantumAnimationApp.js",
  "scripts": {
    "hint": "jshint --config jshintConfig.json src",
    "browserify": "browserify index.js -o index.deps.js",
    "uglify": "uglifyjs index.deps.js -o index.min.js",
    "build": "npm run hint && npm run browserify && npm run uglify"
  },
  "repository": {
    "type": "git",
    "url": "none"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "animated-qubits": "0.0.8",
    "jsqubits": "~0.2.0"
  },
  "devDependencies": {
    "browserify": "~3.24.8",
    "uglify-js": "~2.4.11",
    "jshint": "~2.4.2"
  }
}
